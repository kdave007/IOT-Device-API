/**
 * @brief
 * query selection code
 * 1 :just get the update rows of wifi ap table related to one device
 * 2 :get all rows of wifi ap table related to one device
*/

const QUERY = {
     ALL : 0,
     UPDATE : 1,
}

module.exports = {QUERY};